source 'https://rubygems.org'

gem 'rails', '5.1.4' # only to speed up dep. resolution
gem 'pg'
gem 'puma'
gem 'sass-rails'
gem 'uglifier'
gem 'webpacker'
gem 'bcrypt'

group :development, :test do
  gem 'byebug'
  gem 'rspec-rails'
end

group :development do
  gem 'listen'
end
